%p#notice= notice
%h1 Wikis
- if policy(Wiki.new).show?
  %table
    %thead
      %tr
        %th Body
        %th Title
        %th Image
        %th User
        %th{:colspan => "3"}
    %tbody
      - @wikis.each do |wiki|
        %tr
          %td= wiki.body
          %td= markdown_to_html wiki.title
          %td= wiki.image
          %td= wiki.user
          - if policy(Wiki.new).create?
            %td= link_to 'Show', wiki
          %td= link_to 'Edit', edit_wiki_path(wiki)
          %td= link_to 'Destroy', wiki, method: :delete, data: { confirm: 'Are you sure?' }
  %br/
  = link_to 'New Wiki', new_wiki_path